## 2024-05-23 - Smart Defaults reduce friction
**Learning:** Users often perform the same repetitive setup actions (like selecting the Downloads folder). Pre-filling inputs with "Smart Defaults" removes this friction and makes the app feel like it anticipates their needs.
**Action:** Always look for inputs that have a "most likely" value and pre-fill them, while still allowing the user to change it.
